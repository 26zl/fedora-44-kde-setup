# /etc/systemd/system/tmp.mount.d/override.conf — drops the systemd 256+
# per-user usrquota on /tmp (keeps mode/nosuid/nodev/size)
[Mount]
Options=mode=1777,strictatime,nosuid,nodev,size=50%%,nr_inodes=1m
